version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.9.0
    steps:
      - checkout
      - run: ls -d */

  lsdir:
    docker:
      - image: circleci/python:3.9.0
    steps:
      - run: cat flask/Dockerfile

workflows:
  allinall:
    jobs:
      - build
      - lsdir
